.gradle/
.intellijPlatform/
build/
out/
bin/
.idea/
*.iml
*.log
sample-trace-demo/out/
sample-blast-demo/out/
sample-blast-demo/.gradle/
sample-blast-demo/build/

# Agent worktrees / local tooling — never part of the submission
.claude/

# Local secrets — NEVER committed. The API key lives here, not in source.
.ripple-local.properties
*.local.properties
docs/node_modules
docs/package-lock.json
docs/package.json
