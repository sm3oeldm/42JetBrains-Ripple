kotlin.code.style=official
org.gradle.jvmargs=-Xmx2g

# Spec 3.1/5-P0.2: platform pinned here rather than hardcoded in build.gradle.kts.
# NOTE: the machine has IDEA Community 2025.2.6.2 installed, but the build is
# pinned to 2024.3.5 because that is what this code was written and verified
# against. sinceBuild=243 / untilBuild=null means the built plugin still
# installs into 2025.2.x. Do not bump this during the event.
platformVersion=2024.3.5
